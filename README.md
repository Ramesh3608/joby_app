# joby_app
Jobby App is a fully functional job-search application built using React JS. It includes authentication (Login + Protected Routes), profile fetching, job listings with filters, job search using query parameters, and detailed job pages with similar job recommendations.
